{htxt id="is_original_eml_attached-title"}
  {ts}Store original email{/ts}
{/htxt}

{htxt id="is_original_eml_attached"}
  <p>{ts}If enabled CiviCRM will store a copy of the original email in .eml file format as an attachment on the activity.{/ts}</p>
{/htxt}
